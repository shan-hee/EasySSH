# NPM配置文件
# 优化Docker构建中的npm安装过程

# 使用官方registry
registry=https://registry.npmjs.org/

# 禁用进度条（在CI环境中）
progress=false

# 禁用审计（加速安装）
audit=false

# 禁用资金信息
fund=false

# 设置日志级别
loglevel=warn

# 禁用更新检查
update-notifier=false

# 设置缓存目录
cache=/tmp/.npm

# 设置超时时间
timeout=60000

# 禁用可选依赖（如果构建失败）
# optional=false

# 使用生产环境配置
production=false

# 解决依赖冲突
legacy-peer-deps=true
